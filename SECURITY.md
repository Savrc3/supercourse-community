# 安全政策

## 报告安全问题

请不要在公开 Issue 中披露可利用的漏洞、令牌、密码、数据库、日志或真实用户数据。请通过仓库维护者提供的私下渠道报告，并说明影响范围、复现步骤和临时缓解办法。

## 部署边界

- 公开源码不包含任何个人服务器地址、账号、QQ 目标、密钥、数据库或构建签名材料。
- 自建实例应使用 HTTPS、关闭公开注册、限制 CORS，并将 `.env`、SQLite 数据和媒体目录放在仓库外。
- 不要把生产数据库、备份、教务原文件或通知配置提交到 Git。
- 生产环境建议关闭 OpenAPI 文档和详细健康信息，并定期备份数据。

## 支持版本

只对最新稳定版本提供安全修复。个人实例和自建实例由部署者负责更新依赖、证书和备份。
