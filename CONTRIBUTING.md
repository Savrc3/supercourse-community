# 贡献指南

欢迎提交问题和改进建议。

1. 先搜索已有 Issue，避免重复创建。
2. 新功能先说明使用场景、最小闭环和验收方式。
3. 修改前端页面时补 Vitest 或 Playwright 回归，并检查 360×800 窄屏。
4. 修改后运行 `scripts/check-all.ps1`，不要提交 `.env`、`data/`、日志、构建产物、真实教务文件或密钥。
5. 提交使用 Conventional Commits；Pull Request 写清楚改动、原因和验证命令。

项目支持两种运行形态：不依赖服务器的本地模式，以及用户自己部署后端的同步模式。请不要把项目维护者的个人实例当作公共后端依赖。
